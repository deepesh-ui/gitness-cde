{
  "image": "mcr.microsoft.com/devcontainers/base:dev-ubuntu-24.04",
  "containerEnv": {
    "DEBIAN_FRONTEND": "noninteractive"
  },
  "postStartCommand": "sudo apt update && sudo apt install  -y nodejs && sudo apt install  -y npm && sudo npm install --global yarn"
}
